# Main article PDFs

Replace each document template with the corresponding main article PDF.
Filenames use the DOI with `/` replaced by `_`, for example
`10.1021_jacs.2c09756.pdf`. Keep `../inventory.csv` consistent with the paper set.
See the [input instructions](../README.md) before running extraction.
