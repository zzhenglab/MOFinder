# Preserve dataset bytes across platforms so recorded file hashes remain stable.
*.csv -text
*.xlsx binary
*.jsonl -text
prompts/*.txt -text
prompts/**/*.txt -text
# Configuration and provenance hashes must match on Windows and Linux.
*.json -text
