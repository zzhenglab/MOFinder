# Preserve dataset bytes across platforms so recorded file hashes remain stable.
*.csv -text
*.xlsx binary
*.jsonl -text
prompts/*.txt -text
prompts/**/*.txt -text
# Configuration and provenance hashes must match on Windows and Linux.
*.json -text
# The literature classification manifest records this source file's byte hash.
tools/literature_triage/topic_classifier.py -text
